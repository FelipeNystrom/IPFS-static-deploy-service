{
  "name": "ipfs-ethereum",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client-dev": "cd client && npm start",
    "client-build": "cd client && npm run build",
    "ipfs-deploy": "NODE_ENV=development node -r dotenv/config ipfs.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^6.1.0",
    "ipfs-http-client": "^32.0.1",
    "node-sass": "^4.13.1"
  }
}
