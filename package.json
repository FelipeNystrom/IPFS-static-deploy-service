{
  "name": "ipfs-ethereum",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev-client": "cd client && npm start",
    "build-client": "cd client && npm run build",
    "ipfs": "NODE_ENV=development nodemon -r dotenv/config ipfs.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^6.1.0",
    "ipfs-api": "^26.1.2",
    "node-sass": "^4.10.0"
  }
}
